language: android
jdk: oraclejdk8
android:
  components:
  - platform-tools
  - tools
  - build-tools-25.0.2
  - android-N
  - extra
before_install:
  - chmod +x gradlew
script:
- ./gradlew assembleRelease